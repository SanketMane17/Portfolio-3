# Server Configuration
NODE_ENV=production
PORT=5000

# Database (PostgreSQL)
# Only needed if using backend - for client-only deployment on Netlify, this is optional
DATABASE_URL=postgresql://user:password@localhost:5432/portfolio_db
